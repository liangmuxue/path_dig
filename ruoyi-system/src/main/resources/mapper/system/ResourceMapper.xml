<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruoyi.system.mapper.ResourceMapper">
    <insert id="insertResourceRef">
        INSERT INTO sys_role_resource
            (ROLE_ID,RESOURCE_ID)
        VALUES
            (#{roleId}, #{resourceId})

    </insert>
    <delete id="deleteResourceRefByRoleId">
     delete from sys_role_resource where ROLE_ID =#{roleId}
    </delete>

    <select id="selectResource" resultType="com.ruoyi.common.core.domain.entity.Resource">
        SELECT
            RESOURCE_ID as resourceId,
            APP_ID as appId,
            RES_CODE as resCode,
            RES_CANME as resCanme,
            RES_ENAME as resEname,
            STATUS as status,
            REMARK as remark,
            RES_TYPE as resType,
            RES_ORDER as resOrder,
            URL as url,
            RES_ICO as resIco,
            PARENT_ID as parentId
        from sys_resource
        where STATUS = 1
    </select>


    <select id="selectResourceByCondition" parameterType="com.ruoyi.common.core.domain.entity.Resource" resultType="com.ruoyi.common.core.domain.entity.Resource">
        select
        RESOURCE_ID as resourceId,
        APP_ID as appId,
        RES_CODE as resCode,
        RES_CANME as resCanme,
        RES_ENAME as resEname,
        STATUS as status,
        REMARK as remark,
        RES_TYPE as resType,
        RES_ORDER as resOrder,
        URL as url,
        RES_ICO as resIco,
        PARENT_ID as parentId
        from sys_resource
        <where>
            STATUS = 1
            <if test="resCodes != null and resCodes != ''">
                and find_in_set(RESOURCE_ID, #{resCodes})
            </if>
            <if test="resType != null">
                and RES_TYPE &lt;= #{resType}
            </if>
        </where>
    </select>
    <select id="selectResourceIdByRoleId" resultType="java.lang.String">
        select RESOURCE_ID from sys_role_resource where ROLE_ID = #{roleId}
    </select>


</mapper>